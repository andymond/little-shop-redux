<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Items Dashboard</title>
    <link href="https://fonts.googleapis.com/css?family=Press+Start+2P" rel="stylesheet">
    <link href="/stylesheets/main.css" type="text/css" rel="stylesheet">
  </head>
  <body>
    <a href="/items">All Items</a>
    <a href="/merchants">All Merchants</a>

    <div class="dashboard">
      <h2>Total Items: <%= @items.count %> </h2>
      <h2>Average Price of Items: <%= @items.average(:price).round(2) %> </h2>
      <h2>Newest Item: <%= @items.last.title %> </h2>
      <h2>Oldest Item: <%= @items.first.title %> </h2>
    </div>
  </body>
</html>
