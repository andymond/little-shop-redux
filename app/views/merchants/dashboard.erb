<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Merchants Dashboard</title>
    <link href="https://fonts.googleapis.com/css?family=Press+Start+2P" rel="stylesheet">
    <link href="/stylesheets/main.css" type="text/css" rel="stylesheet">
  </head>
  <body>
    <a href="/items">All Items</a>
    <a href="/merchants">All Merchants</a>

    <div class="dashboard">
      <% merchant = @items.group(:merchant_id).order('count(*)DESC').pluck(:merchant_id).first %>
      <h2>Merchant with the most items: <%= @merchants.find_by(id: merchant).name %> </h2>
      <h2>Merchant with the highest priced item: <%= @items.find_by(price: (@items.maximum(:price))).merchant.name %></h2>
    </div>
  </body>
</html>
