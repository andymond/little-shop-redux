<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Merchants Dashboard</title>
    <link href="https://fonts.googleapis.com/css?family=Press+Start+2P" rel="stylesheet">
    <link href="/stylesheets/main.css" type="text/css" rel="stylesheet">
  </head>
  <body>
    <a href="/items">All Items</a>
    <a href="/merchants">All Merchants</a>

    <div class="dashboard">
      <h2>Merchant with the most items: <%= @items.merchant_with_most_items.name %> </h2>
      <h2>Merchant with the highest priced item: <%= @items.merchant_with_highest_priced_item.name %></h2><br />

      <% @merchants.each do |merchant| %>
        <% merc_total_price = merchant.items.map(&:price).sum %>
        <h2> <%= merchant.name %> total items: <%= merchant.items.count %> </h2>
        <h2> <%= merchant.name %> total price of items: <%= merc_total_price %> </h2><br />
      <% end %>
    </div>
  </body>
</html>
